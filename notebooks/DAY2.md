# Day 2 — 決定木を「実物」で動かす

## いまどこ？（Day 1 のおさらい）
Day 1 は**対話だけ**で、決定木の学習のしくみを最後まで自分の言葉で組み立てた。
到達点 = 「良い分割 = 2クラスがいちばんキレイに分かれる線を、勘ではなく**測定**で選ぶ」。
でもまだ**一行もコードを書いていない**＝全部「頭の中」。

## 今日のゴール
1. Day 1 で頭の中で組み立てた決定木が、**現実でも同じ形で動くか**を自分の目で確認する（検証）
2. Kaggle で毎日使う道具（pandas / scikit-learn）に**初めて手を触れる**

## 題材：Day 1 の自分の例をそのままデータに
`data/chat_sessions.csv`（400件のチャットセッション）。

| 列 | 意味 |
|---|---|
| `turns` | やり取り数（Day 1 の主役の手がかり） |
| `used_summary` | まとめボタン押下（0/1） |
| `stay_minutes` | 滞在時間。**Day 1 で「使わなかった」囮**。機械もこれを捨てる？ |
| `deepened` | **本当の答え**（深まった=1 / 否=0）＝ ground truth |

中の「隠れルール」は Claude しか知らない。
→ **機械はこのデータから、Day 1 で自分が手で描いた木を再発見できるか？**

## あなたがやること（本筋＝自分で書く）
ノートブック `notebooks/day2_decision_tree.ipynb` の 🎯セルを埋める。完成形は渡されない。

3ステップ：
1. 手がかり `X` と 正解 `y` に分ける（`df[["列",...]]` と `df["列"]`）
2. 決定木の箱を作る（`DecisionTreeClassifier(...)`、最初は `max_depth` で浅く）
3. 学習させる（Day 1 の「`fit` で学習させる」。何を渡す？）

## 進め方（リモート操作中なので）
GUI は触らなくてOK。
1. まず**予想**を言葉で：一番上の手がかりは？／`turns` の線は何回あたり？／`stay_minutes` は使われる？捨てられる？
2. 🎯の3〜4行を**チャットに貼る**だけ。Claude が実行して木の絵を見せる。

→ 予想と「機械が出した木」で答え合わせ。そこで見える数字が、次回の「不純度／ジニ」への入口になる。
